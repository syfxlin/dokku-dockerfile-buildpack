{% import '../inject.twig' as i %}
{% include '../base.twig' %}

{# Install nginx #}
{% include '../nginx/_install.twig' %}

{# Copy project #}
WORKDIR /app
{# Copy by nginx.root #}
{% set nginx_root = nginx.root | default('public') %}
COPY --from=builder /app/{{ nginx_root }} ./{{ nginx_root }}
{% include '../nginx/_copy.twig' %}

{# Inject dockerfile #}
{{ i.inject(nginx.run) }}

{# Define #}
{% include '../nginx/_define.twig' %}
