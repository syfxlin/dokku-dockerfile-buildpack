{# Install nginx #}
{% include '../nginx/_install.twig' %}

{# Copy project #}
{% include '../nginx/_copy.twig' %}

{# Inject dockerfile #}
{{ i.inject(nginx.run) }}

{# Install supervisor #}
COPY .buildin/supervisor.conf /etc/supervisor/supervisor.conf
RUN apk add --no-cache supervisor

{# Define image #}
EXPOSE 80
CMD ["/usr/bin/supervisord", "-c", "/etc/supervisor/supervisor.conf"]
